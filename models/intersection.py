class Intersection:
    def __init__(self, _id, street_id, street_name, origin_id, final_id,
                 origin_intersection_id, final_intersection_id, distance,
                 speed, cost_1, cost_2, origin_lat, origin_lon, final_lat,
                 final_lon):
        _id = _id
        street_id = street_id
        street_name = street_name
        origin_id = origin_id
        final_id = final_id
        origin_intersection_id = origin_intersection_id
        final_intersection_id = final_intersection_id
        distance = distance
        speed = speed
        cost_1 = cost_1
        cost_2 = cost_2
        origin_lat = origin_lat
        origin_lon = origin_lon
        final_lat = final_lat
        final_lon = final_lon
